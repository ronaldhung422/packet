# 🎨 Albo 風格重構 - 前後對比

## 1. 主要差異總覽

### 🎨 視覺設計變更

#### 之前 (舊版本)
- ❌ 標準 Tailwind 配色（藍色/灰色為主）
- ❌ 小圓角 (8px)
- ❌ 簡單的卡片設計
- ❌ 沒有漸變色
- ❌ 基本的列表佈局

#### 之後 (Albo 風格)
- ✅ 紫色/粉紅漸變主題色 (#6750A4, #E91E63)
- ✅ 大圓角 (16-20px)
- ✅ 精緻的卡片陰影和 hover 效果
- ✅ 柔和的漸變背景
- ✅ 現代化的 2-column grid 佈局

---

### 📱 首頁 (Home.tsx) 變更

#### 之前
```
- 歡迎橫幅
- 4個快速操作按鈕 (2x2 grid)
  * 新增地點
  * 地圖檢視
  * 所有地點
  * 統計
- 最近新增 (垂直列表，最多3個)
- 快速統計卡片
- 使用提示區塊
```

#### 之後
```
- 橫向滑動 Filter Chips (🍽️ 全部、🍜 中餐、☕ 咖啡...)
- 最近 Saved Carousel (橫向滑動，顯示8個)
- Collections 區塊標題
- 2-column Collections Grid
  * 📁 全部
  * 🤔 想去試試
  * ✅ 去過了
  * ⭐ 最愛
  * 🇭🇰 香港美食
  * ☕ 咖啡館
  * 🍰 甜品店
- 快速統計區塊（保留）
- FAB 浮動按鈕 (右下角紫色 +)
- Bottom Sheet 新增選單
```

**移除的內容：**
- ❌ 4個快速操作按鈕
- ❌ 使用提示區塊

**新增的內容：**
- ✅ Filter Chips (emoji 篩選)
- ✅ Carousel (橫向滑動)
- ✅ Collections Grid (2-column)
- ✅ FAB + Bottom Sheet

---

### 🏷️ Collections 系統 (全新功能)

#### 之前
- ❌ 沒有 Collections 概念
- ❌ Places 只能標記 3 種狀態：想去試試、去過了、最愛
- ❌ 無法自訂分類

#### 之後
- ✅ 7 個預設 Collections（可擴充）
- ✅ 一個 Place 可以屬於多個 Collections
- ✅ 可建立、編輯、刪除自訂 Collections
- ✅ 每個 Collection 有獨立頁面
- ✅ 支援 Drag & Drop 跨 Collection 移動
- ✅ Collections 顯示餐廳數量和封面圖

---

### 👥 用戶識別系統

#### 之前
```typescript
place.addedBy: 'ronald' | 'kerry'  // 簡單標記誰新增的
```

#### 之後
```typescript
place.discoveredBy: 'ronald' | 'kerry'  // 改名為「發現者」
place.likedBy: ('ronald' | 'kerry')[]   // 新增：追蹤誰按讚
currentUser: 'ronald' | 'kerry'         // 新增：裝置預設用戶
```

**新功能：**
- ✅ 每張卡片顯示發現者頭像 (👨/👩)
- ✅ Like 按鈕 (❤️)
- ✅ 裝置用戶設定（Settings 可切換）
- ✅ 追蹤誰喜歡哪些餐廳

---

### 🃏 PlaceCard 變更

#### 之前
```
- 基本卡片設計
- 名稱、描述、標籤
- 分類徽章
- 地址、新增時間
- 文字顯示「由 Ronald/Kerry 新增」
- 更多選單 (⋮)
```

#### 之後
```
- 封面圖片 (如果有) - 4:3 比例
- 發現者頭像徽章 (左下角覆蓋在圖片上)
- Like 按鈕 ❤️ (右下角覆蓋在圖片上)
- 名稱、描述、標籤
- 地址、新增時間
- Like 數量顯示
- 更大的圓角和陰影
- Hover 時有浮起效果
```

---

### ➕ 新增餐廳流程變更

#### 之前
```
1. 點擊「新增地點」按鈕
2. Step 1: 貼上 Link 或選擇手動輸入
3. Step 2: 填寫表單（名稱、描述、標籤、分類...）
4. 儲存後跳轉到 /places 頁面
```

#### 之後
```
1. 點擊 FAB (右下角紫色 +)
2. Bottom Sheet 彈出 4 個選項：
   - 📋 貼上 IG/Threads Link
   - 📷 從 Instagram 匯入
   - 🧵 從 Threads 匯入
   - ✍️ 手動輸入
3. Step 1: 貼上 Link
4. Step 2: 填寫表單 + **選擇 Collections（多選必填）**
5. 儲存後跳轉到首頁 (/)
```

**新增：**
- ✅ FAB 快速新增
- ✅ Bottom Sheet 選單（手機友善）
- ✅ Collections 多選器
- ✅ 必須選擇至少一個 Collection

---

### 📁 Collection Detail 頁面 (全新)

**新頁面路徑：** `/collection/:id`

功能：
- ✅ 顯示 Collection emoji 和名稱
- ✅ 餐廳數量統計
- ✅ 2-column 餐廳卡片 grid
- ✅ 每張卡片可拖拽
- ✅ 拖拽時顯示其他 Collections 作為 drop zone
- ✅ 編輯 Collection (名稱、emoji)
- ✅ 刪除 Collection（餐廳不會被刪除）
- ✅ FAB 新增餐廳到此 Collection

---

### 🗄️ Database Schema 變更

#### 新增 Tables

```sql
-- 1. packet_profiles (用戶資料)
CREATE TABLE packet_profiles (
  user_id uuid PRIMARY KEY,
  display_name text NOT NULL,
  avatar_emoji text,
  created_at timestamptz DEFAULT now()
);

-- 2. packet_place_collections (收藏夾)
CREATE TABLE packet_place_collections (
  id uuid PRIMARY KEY,
  collection_id uuid REFERENCES packet_collections(id),
  name text NOT NULL,
  emoji text NOT NULL,
  cover_image text,
  display_order int DEFAULT 0,
  created_by uuid REFERENCES auth.users(id),
  created_at timestamptz DEFAULT now()
);
```

#### Places Data 變更

```typescript
// 之前
{
  addedBy: 'ronald' | 'kerry'
}

// 之後
{
  discoveredBy: 'ronald' | 'kerry',
  collectionIds: string[],           // 新增
  likedBy: ('ronald' | 'kerry')[],   // 新增
  coverImage?: string                 // 新增
}
```

---

### 🎯 UI Components 新增

| Component | 說明 | 用途 |
|-----------|------|------|
| `AvatarBadge` | 顯示 👨/👩 頭像 | PlaceCard, Collection Detail |
| `FilterChips` | 橫向滑動 emoji 篩選 | Home 頁面 |
| `FAB` | 浮動動作按鈕 | 全局快速新增 |
| `BottomSheet` | 底部彈出式選單 | 新增選項、Collection 選擇器 |
| `CollectionCard` | Collection 卡片 | Home 頁面 grid |
| `PlaceCarousel` | 橫向滑動餐廳輪播 | Home 頁面最近 Saved |

---

### 📊 統計頁面保留項目

**保留但不顯著展示：**
- 總地點數
- 按分類統計
- Ronald/Kerry 發現數量
- 最近新增列表

**你想移除的內容（稍後處理）：**
- ❌ 競爭分數系統
- ❌ 排行榜
- ❌ 「誰領先」顯示
- ❌ 積分計算

---

### 🎨 CSS Variables 新增

```css
/* Albo 風格色彩系統 */
--primary-purple: #6750A4
--primary-purple-dark: #4A3A7A
--accent-pink: #E91E63
--accent-light: #D0BCFF
--bg-main: #FFFFFF
--bg-secondary: #F8F8FA
--text-primary: #1A1A1A
--text-secondary: #6B6B6B
--border-light: #E5E5E7
--shadow-sm: 0 2px 8px rgba(0,0,0,0.04)
--shadow-md: 0 4px 16px rgba(0,0,0,0.08)
--radius-lg: 20px
--radius-md: 16px
--radius-sm: 12px
```

---

## 總結：主要視覺變化

1. **配色**：藍灰 → 紫粉漸變
2. **圓角**：8px → 16-20px
3. **佈局**：垂直列表 → 橫向 Carousel + 2-column Grid
4. **互動**：靜態按鈕 → FAB + Bottom Sheet + Drag & Drop
5. **架構**：單層分類 → 多層 Collections 系統
6. **社交**：單向記錄 → 雙向互動（Like、發現者頭像）

---

## 2. 移除統計/分數功能

我現在幫你移除不需要的統計和分數相關內容。

## 3. 為什麼用 localhost 而不是 Vercel？

**原因：**

1. **開發測試**：localhost 是在你電腦本地運行，可以即時看到修改效果
2. **還未部署**：新的 Albo 風格版本還沒有推送到 Vercel
3. **需要先測試**：確保所有功能正常後才部署到正式環境

**部署到 Vercel 的步驟：**

```bash
# 1. 確認所有更改正常
npm run build

# 2. 提交到 Git
git add .
git commit -m "Refactor: Albo style redesign with Collections"

# 3. 推送到 GitHub (會自動觸發 Vercel 部署)
git push origin main

# 4. 等待 Vercel 自動部署完成 (~2-3 分鐘)
# 然後訪問 https://packet-kappa.vercel.app/
```

要我現在幫你：
1. ✅ 移除統計/分數功能
2. ✅ 準備部署到 Vercel 的步驟

?
